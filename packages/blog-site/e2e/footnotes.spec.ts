import { test, expect } from '@playwright/test';

// Footnote tests require an essay with footnotes.
// Currently no essays are published — these tests are placeholders
// that should be re-enabled when essay content is added back.

test.describe('Footnotes', () => {
  test.skip('footnote markers are visible (needs essay content)', async () => {
    // Re-enable when an essay with footnotes exists
  });
});
